<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.taskmanager.task.mapper.TaskCustomMapper">

    <!-- タスク＋カテゴリー情報を含むResultMap -->
    <resultMap id="taskWithCategoryResultMap" type="com.example.taskmanager.task.dto.TaskDto">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="description" property="description" />
        <result column="status" property="status" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="priority" property="priority" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="category_id" property="categoryId" />
        <result column="category_name" property="categoryName" />
        <result column="category_color" property="categoryColor" />
        <result column="due_date" property="dueDate" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- タスクエンティティ用ResultMap -->
    <resultMap id="taskResultMap" type="com.example.taskmanager.common.entity.Task">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="description" property="description" />
        <result column="status" property="status" />
        <result column="priority" property="priority" />
        <result column="category_id" property="categoryId" />
        <result column="due_date" property="dueDate" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 全件取得（カテゴリー情報含む） -->
    <select id="selectAllWithCategory" resultMap="taskWithCategoryResultMap">
        SELECT
            t.id,
            t.title,
            t.description,
            t.status,
            t.priority,
            t.category_id,
            c.name AS category_name,
            c.color AS category_color,
            t.due_date,
            t.created_at,
            t.updated_at
        FROM tasks t
        LEFT JOIN task_categories c ON t.category_id = c.id
        ORDER BY t.created_at DESC
    </select>

    <!-- 条件検索（カテゴリー情報含む） -->
    <select id="selectByConditionWithCategory" resultMap="taskWithCategoryResultMap">
        SELECT
            t.id,
            t.title,
            t.description,
            t.status,
            t.priority,
            t.category_id,
            c.name AS category_name,
            c.color AS category_color,
            t.due_date,
            t.created_at,
            t.updated_at
        FROM tasks t
        LEFT JOIN task_categories c ON t.category_id = c.id
        <where>
            <if test="status != null and status != ''">
                AND t.status = #{status}
            </if>
            <if test="priority != null and priority != ''">
                AND t.priority = #{priority}
            </if>
            <if test="categoryId != null">
                AND t.category_id = #{categoryId}
            </if>
        </where>
        ORDER BY t.created_at DESC
    </select>

    <!-- ID検索（カテゴリー情報含む） -->
    <select id="selectByIdWithCategory" resultMap="taskWithCategoryResultMap">
        SELECT
            t.id,
            t.title,
            t.description,
            t.status,
            t.priority,
            t.category_id,
            c.name AS category_name,
            c.color AS category_color,
            t.due_date,
            t.created_at,
            t.updated_at
        FROM tasks t
        LEFT JOIN task_categories c ON t.category_id = c.id
        WHERE t.id = #{id}
    </select>

    <!-- 全件取得 -->
    <select id="selectAll" resultMap="taskResultMap">
        SELECT id, title, description, status, priority, category_id, due_date, created_at, updated_at
        FROM tasks
        ORDER BY created_at DESC
    </select>

    <!-- 条件検索 -->
    <select id="selectByCondition" resultMap="taskResultMap">
        SELECT id, title, description, status, priority, category_id, due_date, created_at, updated_at
        FROM tasks
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="priority != null and priority != ''">
                AND priority = #{priority}
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- ID検索 -->
    <select id="selectById" resultMap="taskResultMap">
        SELECT id, title, description, status, priority, category_id, due_date, created_at, updated_at
        FROM tasks
        WHERE id = #{id}
    </select>

</mapper>
